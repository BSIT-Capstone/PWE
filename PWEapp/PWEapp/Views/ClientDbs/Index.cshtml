@model IEnumerable<PWEapp.Models.ClientDb>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LocaleID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PriceGroupID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WebChg)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LocaleID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PriceGroupID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WebChg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductID })
        </td>
    </tr>
}

</table>
